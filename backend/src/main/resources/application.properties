# Application identity used by Spring Boot logging and diagnostics.
spring.application.name=extension

# Gemini Generate Content endpoint. The key comes from GEMINI_KEY, never source code.
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=
gemini.api.key=${GEMINI_KEY}
